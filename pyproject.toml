[project]
name = "sudoku_prover"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "pantograph",
]

[tool.uv.sources]
pantograph = { git = "https://github.com/stanford-centaur/PyPantograph" }
