On this branch. The next/only task is to un-hardcode the final proof

Basically, when the finish command is issued, need to change the finish command to
1. define the solution function g
2. iterate through each constraint of the puzzle in the order it was defined in lean structure
    (I'm no realizing that it was set up as a dictionary of constraints, so we need to garuntee order in some way
    I would personally like it to just stay in the order that they were defined in the suko file.
    There is extra processing here to convert list of tuple to dict and back.)
3. prove it with known proofs, UniqueSet is Inj.card_on; decide, givens are decide

Need to keep thinking about how constraints are stored to making constraints and theorems easier to implement
Options
- keep the 2 seperate dictionaries to handle the top level (exactly whan lean sees) and the qualified names full constraints
- have one dict that stores them all, and have some sort of boolean to show which are in top level or not...


