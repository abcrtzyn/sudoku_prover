There is one small refactor to handle running through proof steps in main.
I think we need a run command function inside proof_engine that stores the prompt in self.prompt for other programs to pick up state wise.
Basically proof_engine handles it's own generator rather than another program

Merge in the other branches for testing
One thing that will happen later is the default value for outside grid needs to be variable for different symbol types
Check that the proof can be done with that
Also I moved the outside grid hypothesis to the top of the structure def, so that proof needs to move in 2 places

Then recursive importing?
